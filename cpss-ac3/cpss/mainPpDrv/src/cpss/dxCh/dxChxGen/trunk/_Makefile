# $(warning "Started") 
#LIB_NAME = cpssDxChTrunk.lib

#ifneq (L_ON,$(LOG_ENABLE))
#    C_EXCLUDE_FILE_LIST += prvCpssDxChTrunkLog.c prvCpssDxChTrunkManualLog.c
#endif   

#List of subfolders to include in build
MODULES := 

#List of files to include in list
FILES := \
 cpssDxChTrunk.c
ifeq (L_ON,$(LOG_ENABLE))
FILES += \
 prvCpssDxChTrunkLog.c \
 prvCpssDxChTrunkManualLog.c 
endif


CWD := $(dir $(lastword $(MAKEFILE_LIST)))

#Add relative path to original Makefile, to each file in the list
MAINPPDRV_SRC += $(patsubst %,$(CWD)%,$(FILES))

#Enable this line to add include path of this folder
#CFLAGS := $(CFLAGS) -I$(CWD)

#Recursively go into list of subfolders
include $(patsubst %,$(CWD)%/$(MAKEFILE),$(MODULES))

