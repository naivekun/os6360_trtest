# $(warning "Started")
#SW_ROOT_DIR = $(subst \,/,$(SW_ROOT))
#INCLUDE_PATH += -I$(SW_ROOT_DIR)/common/src/cpss/common/labServices/port/gop/src/port/serdes/avago/aapl/marvell

#List of files to include in list
FILES := \
 wrDDRTrainingIP.c \
 wrHwsCommon.c \
 wrHwsDdr3Bap.c \
 wrHwsDebug.c \
 wrHwsMac.c \
 wrHwsPcs.c \
 wrHwsPort.c \
 wrHwsPortAp.c \
 wrHwsSerdes.c \
 wrSerdesExt.c

ifneq (EXISTS, $(PM_FAMILY))
    C_EXCLUDE_FILE_LIST = wrDDR3.c
        FILES := \
         wrDDRTrainingIP.c \
         wrHwsCommon.c \
         wrHwsDdr3Bap.c \
         wrHwsDebug.c \
         wrHwsMac.c \
         wrHwsPcs.c \
         wrHwsPort.c \
         wrHwsPortAp.c \
         wrHwsSerdes.c \
         wrSerdesExt.c
endif

# limit files only if DX not defined
ifneq (EXISTS, $(CHX_FAMILY))
ifeq (EXISTS, $(PX_FAMILY))
        FILES := \
         wrHwsCommon.c \
         wrHwsMac.c \
         wrHwsPcs.c \
         wrHwsPort.c \
         wrHwsSerdes.c \
         wrSerdesExt.c
endif
endif

ifeq (YES, $(CPP_LINK))
    mainGaltisWrapper_AD_CFLAGS+= -DMV_HWS_INCLUDE_INTERFACE_TEST
endif

#List of subfolders to include in build
MODULES :=




CWD := $(dir $(lastword $(MAKEFILE_LIST)))

#Add relative path to original Makefile, to each file in the list
MAINGALTISWRAPPER_SRC += $(patsubst %,$(CWD)%,$(FILES))

#Enable this line to add include path of this folder
#CFLAGS := $(CFLAGS) -I$(CWD)

#Recursively go into list of subfolders
include $(patsubst %,$(CWD)%/$(MAKEFILE),$(MODULES))

